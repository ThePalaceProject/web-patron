import { heading } from "./typography";

const styles = {
  /**
   * root wraps the whole app and these are therefore
   * the default styles
   */
  root: {
    fontFamily: "body",
    fontSize: 0,
    lineHeight: "body",
    fontWeight: "body",
    color: "text"
  }
};

export default styles;
